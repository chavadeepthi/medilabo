spring.application.name=medilaboui
server.port=8082
server.forward-headers-strategy=framework

# ---------- API URLs through Gateway ----------
# Gateway handles routing to backend APIs
backend.api.url=${PATIENT_SERVICE_URL}/api/patient
backend.all.api.url=${PATIENT_SERVICE_URL}/api/patient/all
medical.notes.api.url=${NOTES_SERVICE_URL}/api/patients
